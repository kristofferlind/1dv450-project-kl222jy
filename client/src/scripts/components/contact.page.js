'use strict';

var React = require('react/addons');

var ContactPage = React.createClass({
  render: function() {
    return (
        <main className="container">
            <h1>Contact</h1>
            <p>Only here to showcase routing.</p>
        </main>
    );
  }
});

module.exports = ContactPage;
